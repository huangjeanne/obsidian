# Video Generator Skill 优化建议（正确版本）

**重要说明**：使用原生Google Gemini API和Nano Banana Pro模型，不使用OpenRouter

---

## 🔴 优先级1：修复Gemini图像生成

### 当前问题
```python
# cover_generator.py 当前配置
model: str = "gemini-2.0-flash-exp"  # ❌ 不稳定，返回文本
```

### 解决方案

#### 选项1：切换到gemini-2.5-pro-exp模型
```python
# 修改cover_generator.py第53行
model: str = "gemini-2.5-pro-exp"  # ✅ 更稳定的图像生成
```

#### 选项2：改进prompt工程
```python
# 在generate_content()调用中明确要求图像
prompt = f"""Generate: {prompt}

Important: Return ONLY the image, no text explanation."""

# 或者使用中文
prompt = f"""请生成图像: {prompt}

重要提示：只返回图像，不要返回任何文字说明。"""
```

#### 选项3：添加图像生成配置
```python
# 修改_generate_with_retry方法
generation_config = genai.types.GenerationConfig(
    temperature=0.4,
    top_p=0.8,
    top_k=40,
    # 添加图像生成特定配置
    response_mime_type="image/png"  # 明确要求图像响应
)

response = self.client.models.generate_content(
    model=self.model,
    contents=prompt,
    generation_config=generation_config
)
```

---

### 实施步骤

1. **测试新模型**
```bash
cd ~/.claude/skills/video-generator

# 测试gemini-2.5-pro-exp（如果可用）
python3 scripts/cover_generator.py \
  --action cover \
  --title "测试" \
  --model gemini-2.5-pro-exp \
  --output test_new_model.png
```

2. **修改默认模型**
```bash
# 编辑cover_generator.py
vim ~/.claude/skills/video-generator/scripts/cover_generator.py

# 找到第53行，修改：
# model: str = "gemini-2.5-pro-exp"  # 或其他可用模型
```

3. **验证修复**
```bash
python3 scripts/cover_generator.py \
  --action cover \
  --title "自动化采摘机器人" \
  --subtitle "科技让农业更智能" \
  --style handdrawn \
  --output verify_fix.png
```

---

## 🟡 优先级2：升级文本生成到Gemini Pro 3

### 当前配置
```python
# script_generator.py 使用的模型
# 检查当前使用的模型
```

### 优化方案

#### 使用最新的Gemini模型
```python
# 可用的Gemini模型（按优先级排序）
TEXT_GENERATION_MODELS = [
    "gemini-2.5-pro",      # 最新，最强大
    "gemini-2.0-pro",      # 稳定版本
    "gemini-1.5-pro",      # 快速版本
]
```

---

## 🟢 优先级3：其他改进

### 3.1 改进脚本生成提示词
**目标**：生成研究生级别内容

**实施方案**：
```python
# script_generator.py中已有的graduate_prompts.py
# 已经包含了详细的研究生级别提示词

# 使用方式
python3 scripts/script_generator.py \
  --topic "深度学习在农业机器人中的应用" \
  --system-prompt-file scripts/graduate_prompts.py \
  --duration 180 \
  --output 研究生级别_脚本.md
```

### 3.2 实现硬字幕功能
```python
# 使用moviepy烧录字幕
from moviepy.editor import VideoFileClip, TextClip
import pysrt

def burn_subtitles(video_path, srt_path, output_path):
    """将SRT字幕硬编码到视频"""
    video = VideoFileClip(video_path)
    subs = pysrt.open(srt_path)

    # 为每条字幕创建TextClip
    subtitles_clips = []
    for sub in subs:
        txt_clip = TextClip(
            sub.text,
            fontsize=24,
            color='white',
            font='PingFang-SC-Regular'
        ).set_position(('center', 'bottom'))
        subtitles_clips.append(txt_clip)

    # 合成视频
    final = CompositeVideoClip([video] + subtitles_clips)
    final.write_videofile(output_path)
```

---

## 📋 行动清单

### 立即可做（5分钟）
1. ✅ 检查可用的Gemini模型列表
   ```bash
   python3 -c "import google.generativeai as genai; print([m.name for m in genai.list_models() if 'image' in m.name or 'exp' in m.name])"
   ```

2. ✅ 测试不同模型的图像生成
   ```bash
   # 测试gemini-2.5-pro-exp
   python3 scripts/cover_generator.py --model gemini-2.5-pro-exp ...
   ```

### 短期优化（1-2小时）
3. 修改cover_generator.py，使用更稳定的模型
4. 改进prompt工程，明确要求图像输出
5. 添加图像生成配置（temperature, response_mime_type等）

### 中期优化（2-4小时）
6. 使用graduate_prompts.py生成研究生级别内容
7. 实现moviepy硬字幕功能
8. 改进时长预测（添加中文语速系数）

---

## 🎯 推荐优先级

基于您的需求（不使用OpenRouter，使用原生Gemini API）：

### 第一优先级（立即做）
**修复图像生成** - 切换到更稳定的Gemini模型
- 预期时间：30分钟
- 影响最大：解决核心痛点
- 风险最低：只需改模型名称

### 第二优先级
**研究生级别内容** - 使用已有的graduate_prompts.py
- 预期时间：1小时
- 已有实现，只需测试

### 第三优先级
**硬字幕功能** - 使用moviepy
- 预期时间：2小时
- 改善用户体验

---

## 💡 关键要点

1. **不使用OpenRouter** - 只用原生Google Gemini API
2. **文本生成** - 使用Gemini Pro 3（或更新的2.5-pro）
3. **图像生成** - 使用Nano Banana Pro（通过Gemini API）
4. **音频生成** - 继续使用ListenHub Speech API（已验证可用）

---

**下一步建议**：让我先帮您测试可用的Gemini模型，然后修改cover_generator.py

需要我立即开始吗？
